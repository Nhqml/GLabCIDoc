[package]
name = "glabcidoc"
description = "Generate markdown documentation from GitLab CI configuration files"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive", "env"] }
anyhow = "1"
